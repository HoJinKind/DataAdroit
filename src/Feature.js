import React, { Component } from "react";

class Feature extends Component {
    constructor(props) {
        super(props);
        this.state = {
            data
        }
    }
    render() {
        // mean, mode, median, average, sum, all
    }
}